---
secret:
  # Secrets for zmeventnotification.yml

  ESCONTROL_INTERFACE_PASSWORD:

  ZM_PORTAL: http://127.0.0.1/zm
  ZM_API_PORTAL: http://127.0.0.1/zm/api

  ZM_USER: zoneminder-user
  ZM_PASSWORD: zoneminder-password

  ZM_BASIC_USER: zoneminder-user
  ZM_BASIC_PASS: zoneminder-password

  ML_USER: 
  ML_PASSWORD:

  ZM_ML_USER: 
  ZM_ML_PASSWORD:

  # encrypted credentials (enc_key) for ml_routes
  mlapi_one_key: 'BpwLjlpkcYTFOaNy6ggWETGp_6f7VZ16Aac7GULssfc='

  MLAPI_SECRET_KEY: 

  MQTT_USERNAME:
  MQTT_PASSWORD:

  ES_CERT_FILE:
  ES_KEY_FILE:

  PLATEREC_ALPR_KEY:

  PUSHOVER_USER: 
  PUSHOVER_PASS: 
  PUSHOVER_APP_TOKEN: 
  PUSHOVER_USER_KEY: 
  
  HA_SERVER: 
  HA_TOKEN: 
